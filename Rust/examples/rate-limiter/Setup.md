#LINUX

```bash
cargo build
cargo run
sudo make create-container
sudo make start-container
```

#WINDOWS
- install [chocolatet](https://chocolatey.org/install)
- then run:
```bash
choco install make
cargo build
cargo run
sudo make create-container
sudo make start-container
```